{
  "name": "skuftube-backend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "backend": "uvicorn app.main:app --reload --host 0.0.0.0 --port 8000 --reload-dir app"
  }
}
