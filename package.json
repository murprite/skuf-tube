{
  "name": "skuftube-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "frontend": "cd frontend && pnpm run dev",
    "frontend:build": "cd frontend && pnpm run ",
    "backend": "cd backend && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000 --reload-dir app"
  }
}