{
  "name": "skuftube-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "frontend": "cd ./frontend/ && vite",
    "frontend:build": "cd ./frontend/ && vite build",
    "backend": "cd ./backend/ && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000"
  }
}
